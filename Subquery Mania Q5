Q5. For the customer that spent the most (in total over their lifetime as a customer) total_amt_usd, how many web_events did they have for each channel?

select avg(total)
from(
  select account_id, sum(total_amt_usd) total
  from orders
  group by 1
  order by 2 desc
  limit 10) t1
